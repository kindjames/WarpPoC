@model TopNavigationBarViewModel

<div id="navbar" class="navbar navbar-fixed">
    <button type="button" class="navbar-toggle navbar-btn collapsed" data-toggle="collapse" data-target="#sidebar">
        <span class="fa fa-bars"></span>
    </button>
    <a class="navbar-brand" href="@Url.Action("Index", "Home")">
        <small>
            <i class="fa fa-desktop"></i>
            @Model.ApplicationTitle
        </small>
    </a>

    <ul class="nav flaty-nav pull-right">
        @Html.Action("TasksDropdown")

        @Html.Action("NotificationsDropdown")

        @Html.Action("MessagesDropdown")

        @Html.Action("UserDropdown")
    </ul>
</div>